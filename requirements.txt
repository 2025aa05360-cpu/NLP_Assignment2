# Grammatical Error Correction (Assignment 2) - full environment
# Install with:  pip install -r requirements.txt
# Python 3.10+ recommended.

# --- core deep learning / modelling ------------------------------------------
torch>=2.0
transformers>=4.41
datasets>=2.19
accelerate>=0.30
sentencepiece>=0.1.99          # T5 tokenizer backend
protobuf>=3.20                 # needed by the SentencePiece -> fast-tokenizer conversion
huggingface-hub>=0.23          # download backend for load_dataset / from_pretrained

# --- evaluation (Phase 4) -----------------------------------------------------
evaluate>=0.4.0                # metric loader used for BERTScore
bert-score>=0.3.13             # backend of the 'bertscore' metric
jiwer>=3.0                     # CER / WER
scikit-learn>=1.3              # required transitively by evaluate's metric backends
sacrebleu>=2.4                 # optional BLEU/GLEU cross-check on GEC output

# --- data handling / plotting -------------------------------------------------
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
pyarrow>=14.0                  # datasets storage backend
tqdm>=4.66                     # progress bars during map / train / generate

# --- application (Phase 2 deliverable) ----------------------------------------
streamlit>=1.32                # st.toggle and st.divider need >= 1.29

# --- notebook runtime ---------------------------------------------------------
notebook>=7.0
ipykernel>=6.29
ipywidgets>=8.1                # renders Hugging Face progress bars in the notebook

# --- optional: public URL for the Streamlit app on a headless lab node ---------
pyngrok>=7.0
